<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.mappers.employeeMapper">

	<insert id="insertEmployee" parameterType="Employee">
		insert into employee(employee_id, department_id, name, password, email, phone)
		values(#{employeeId}, #{departmentId}, #{name}, #{password}, #{email}, #{phone})
	</insert>

	<select id="selectEmployee" resultType="Employee">
		select * from employee
	</select>

	<select id="selectAccountEmployee" resultType="boolean">
		select count(*) from employee
		where employee_id = #{employee_id} and
		password = #{password}
	</select>
	
	<select id="selectCheckIdEmployee" parameterType="String" resultType="Employee">
		select * from employee
		where employee_id = #{employeeId}
	</select>
	
	<select id="selectCheckEmailEmployee" parameterType="String" resultType="Employee">
		select * from employee
		where email = #{email}
	</select>
</mapper>