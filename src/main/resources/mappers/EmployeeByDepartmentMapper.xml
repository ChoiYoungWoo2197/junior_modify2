<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.mappers.EmployeeByDepartmentMapper">

	<select id="searchEmployeeByDepartment" resultType="EmployeeByDepartment">
		SELECT *, (SELECT count(employee_id) FROM employee WHERE employee.department_id = department.department_id) AS emoloyeeCount
		FROM department WHERE name LIKE '%${searchContent}%' 
		ORDER BY department_id DESC LIMIT #{pageStart}, #{amount}
    </select>
    
    <select id="selectEmployeeByDepartmentById" resultType="EmployeeByDepartment">
    	SELECT department.department_id, department.name, employee.employee_id, employee.name AS employeeName, employee.email, employee.phone 
		FROM department JOIN employee ON employee.department_id = department.department_id
		WHERE department.department_id = #{departmentId}
    </select>
</mapper>