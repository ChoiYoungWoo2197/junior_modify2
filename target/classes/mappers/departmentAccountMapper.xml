<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.mappers.departmentAccountMapper">

	<select id="selectDepartmentAccount" resultType="DepartmentAccount">
		SELECT dpt.department_id, dpt.name, dpt.register_date, (SELECT COUNT(*) FROM employee AS emp WHERE emp.department_id = dpt.department_id) as account
		FROM department AS dpt
	</select>

	<select id="selectSearchDepartmentAccount" parameterType="String" resultType="DepartmentAccount">
		SELECT dpt.department_id, dpt.name, dpt.register_date, (SELECT COUNT(*) FROM employee AS emp WHERE emp.department_id = dpt.department_id) as account
		FROM department AS dpt
		where name like concat(#{name}, '%')
	</select>
</mapper>